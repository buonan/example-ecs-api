{
  "name": "workers",
  "version": "1.0.10",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "compile": "npm version patch && tsc",
    "start": "node ./dist/index.js",
    "build": "npm run compile && docker build -t worker ."
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^18.15.10",
    "typescript": "^5.0.2"
  },
  "dependencies": {
    "kafkajs": "^2.2.4",
    "mongoose": "^7.0.3"
  }
}
